version: '3.8'
services:
  postgres:
    container_name: postgres-employee
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1
    ports:
      - "5432:5432"
